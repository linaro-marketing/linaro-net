# In order to build for staging run:
# $ jekyll build --config _config.yml, _config-production.yml
#
#
# URL of static site
url: "https://www.linaro.net"
# Destination folder
destination: production.linaro.net
# Production boolean used in layouts / includes
production: true
google_analytics:
  enabled: false
  code: UAXXXXXX
