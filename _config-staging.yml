# In order to build for staging run:
# $ jekyll build --config _config.yml, _config-production.yml
#
#
# URL of static site
url: "https://staging.linaro.net"
# Destination folder
destination: staging.linaro.net
# Production boolean used in layouts / includes
production: false
google_analytics:
  enabled: false
  code: UAXXXXXX
